#include "lemin.h"

void init_split(t_farm *orgn, t_farm *new)
{

}

t_path *ft_suurballe(t_farm *orgn, )
{
    t_farm  split;
    t_path  *good;
    int     max_p;
    int     i;

    init_split(orgn, &split);
    good = malloc(sizeof(t_path) * ordn->mpw);
    

}